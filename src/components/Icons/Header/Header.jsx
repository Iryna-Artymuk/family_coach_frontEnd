import Logo from '../../Logo/Logo';
import Container from '@/components/Container/Container';

const Header = () => {
  return (
    <header>
      <Container>
        <Logo />
      </Container>
    </header>
  );
};

export default Header;
